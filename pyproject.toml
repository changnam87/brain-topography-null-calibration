[project]
name = "bt-hyperscanning"
version = "0.1.0"
description = "Brain Topography EEG hyperscanning null-calibration experiments"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
